
<%= image_tag @event.event_image_url(:big_thumb).to_s %>

<%= @event.event_name %>

<%= @event.from_date %>

<%= @event.until_date %>

<%= link_to 'Back', events_path %>

<%= link_to 'Edit', edit_event_path %>

<%= link_to 'Delete', event_path, method: :delete, confirm: "Are you sure?" %>